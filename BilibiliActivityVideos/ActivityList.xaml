<Window x:Class="BilibiliActivityVideos.ActivityList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BilibiliActivityVideos"
        mc:Ignorable="d"
        Title="活动列表" Height="450" Width="350" WindowStyle="ToolWindow" Closed="Window_Closed">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="aList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5" Grid.Row="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
        <TextBlock x:Name="statusBox" HorizontalAlignment="Left" Margin="10,0,0,0" TextWrapping="NoWrap" Text="状态栏" VerticalAlignment="Center" Grid.Row="1"/>
    </Grid>
</Window>
